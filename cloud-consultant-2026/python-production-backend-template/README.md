# Legacy Imported Material

This directory is retained for historical reference only.

It is **not** the active application root and is not presented as a production backend implementation. New work for this repository should use the root-level structure described in [the main README](../../README.md):

```text
app/
tests/
infra/
docs/
.github/workflows/
```

Keeping the legacy material clearly labeled prevents duplicate project roots from confusing reviewers or deployment tooling.